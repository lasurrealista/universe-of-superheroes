import { SorterPipePipe } from './sorter-pipe.pipe';

describe('SorterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SorterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
